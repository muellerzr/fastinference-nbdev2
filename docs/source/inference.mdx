# Inference Learner

`fastinference` revolves around the [`InferenceLearner`] class, which is a new version of the fastai `Learner` designed to make
deployment easier. 

## API Documentation:

[[autodoc]] InferenceLearner